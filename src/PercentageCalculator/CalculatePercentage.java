package PercentageCalculator;

public class CalculatePercentage {
    public static double calculatePercentage(double totalMarks, double totalPossibleMarksOverall)
    {
        return (totalMarks*totalPossibleMarksOverall)*100;
    }
}
